#pragma once

#include <cstddef>
#include <cstdint>
#include <span>

// Slice 1. Read/write primitives over a byte span for the binary capture.
// TODO(fuaad): write this yourself. Declarations first, then the test, then the body.

namespace te {

}  // namespace te


//*
// bool writeU64(std::span<std::byte> buffer, 
// std::size_t offset, std::uint64_t value); / 
// bool readU64(std::span<const std::byte> buffer, 
// std::size_t offset, std::uint64_t& out); — false
//  */