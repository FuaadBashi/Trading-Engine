#pragma once

// Slice 3. Lock-free ring buffer, one producer, one consumer. Power-of-two capacity. Its planned
// public operation is tryPop(T& out), which returns false when the queue is empty.
// TODO(fuaad): write this yourself. Declarations first, then the test, then the body.

namespace te {

}  // namespace te
