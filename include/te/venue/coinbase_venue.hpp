#pragma once

// NOT reserved by any current plan v4 stage. Plan v4 §22 puts a second venue out of scope until
// "after the core project, choose only one specialization" -- this file predates that scope
// discipline and contradicts it. Do not build against this without first deciding whether a
// second venue is still the chosen specialization.

namespace te {}  // namespace te
