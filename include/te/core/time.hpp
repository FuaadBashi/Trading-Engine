#pragma once

// Slice 1. Nanos alias and the Clock interface. Nothing else may call the system clock.
// TODO(fuaad): write this yourself. Declarations first, then the test, then the body.

namespace te {

}  // namespace te
