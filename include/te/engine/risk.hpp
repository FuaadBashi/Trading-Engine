#pragma once

// Slice 5. Position limits, drawdown halt, order rate limit. Runs on BOTH paths.
// TODO(fuaad): write this yourself. Declarations first, then the test, then the body.

namespace te {

}  // namespace te
