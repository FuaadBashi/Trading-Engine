#pragma once

// Slice 3. Feed interface. ReplayFeed and LiveFeed are the only implementations.
// TODO(fuaad): write this yourself. Declarations first, then the test, then the body.

namespace te {

}  // namespace te
