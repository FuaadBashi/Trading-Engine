// Reserved for plan v4 §19 (Stage 9). Same strategy with a live feed/venue; implement test-first
// without changing strategy. Commented out of the build in CMakeLists.txt until then.

int main(int argc, char** argv) {
    (void)argc;
    (void)argv;
    return 0;
}
