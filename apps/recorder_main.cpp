// Slice 1. Connects to the venue, decodes, writes a binary capture.
// Build this LAST in slice 1. Steps 1 to 3 of the slice run off a file, not a socket.
// TODO(fuaad): write this yourself.

int main(int argc, char** argv) {
    (void)argc; (void)argv;
    return 0;
}
