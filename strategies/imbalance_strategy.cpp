// Reserved for plan v4 §16 (Stage 6). Order-book imbalance signal. Only after the simulator
// (Stage 5) is validated, and only as a baseline evaluated against §16's required baselines --
// not before them.
// TODO(fuaad): write this yourself.
